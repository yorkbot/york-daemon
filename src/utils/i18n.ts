/**
 * Localization helper. Simplified to English-only for York system.
 * Kept as a function so we don't have to refactor every call site.
 */
export function L(en: string, _kr?: string): string {
  return en;
}
